### Setup ###
* Unzip the project into a folder of your choice.
* On a terminal, run:
```
$ cd /path/to/project
$ mvn clean install
$ wildfly:run
```
* Open the page **localhost:9090** on your browser.

Dritan.